<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.dao.UserDao">

    <resultMap id="UserResultMap" type="com.company.dao.pojo.User">
        <id property="id" column="ID" jdbcType="INTEGER"></id>
        <result property="username" column="USERNAME" jdbcType="VARCHAR"/>
        <result property="sal" column="SAL" jdbcType="DECIMAL"/>
        <result property="birthday" column="BIRTHDAY" jdbcType="DATE"/>
    </resultMap>

    <sql id="userColumn">id,username,sal,birthday</sql>
    <insert id="save">
        insert into user(<include refid="userColumn"/>) values(#{id},#{username},#{sal},#{birthday})
    </insert>
    <insert id="saveAutoIncrement" useGeneratedKeys="true" keyProperty="id">
        insert into user(username,sal,birthday) values(#{username},#{sal},#{birthday})
    </insert>
    <update id="update" parameterType="com.company.dao.pojo.User">
        update user set username=#{username},sal=#{sal},birthday=#{birthday} where id=#{id}
    </update>
    <delete id="delete">
        delete from user where id=#{userId}
    </delete>

    <select id="findById" resultMap="UserResultMap">
      select <include refid="userColumn"/> from user where id=#{userId}
    </select>
    <select id="findAll" resultType="com.company.dao.pojo.User">
      select <include refid="userColumn"/> from user
    </select>
    <select id="findByName" resultType="com.company.dao.pojo.User">

        select <include refid="userColumn"/> from user where username like concat('%',#{username},'%')

    </select>
    <select id="findByPage" parameterType="int" resultType="com.company.dao.pojo.User">
        select <include refid="userColumn"/> from user limit #{begin},#{size}
    </select>
    <select id="findByPage2" parameterType="map" resultType="com.company.dao.pojo.User">
        select <include refid="userColumn"/> from user limit #{begin},#{size}
    </select>
</mapper>